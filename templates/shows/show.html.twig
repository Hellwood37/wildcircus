{% extends 'base.html.twig' %}

{% block title %}{{ show.Location }}{% endblock %}

{% block body %}
    <h1>{{ show.Location }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Location</th>
                <td>{{ show.Location }}</td>
            </tr>
            <tr>
                <th>Get_tickets</th>
                <td><a href="{{ show.GetTickets }}">Get tickets</a></td>
            </tr>
            <tr>
                <th>Concert_hall</th>
                <td>{{ show.concertHall }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ show.Date ? show.Date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <button type="button" class="btn btn-warning"><a href="{{ path('shows_index') }}">back to list</a></button>                   
    <button type="button" class="btn btn-warning"><a href="{{ path('shows_edit', {'id': show.id}) }}">edit</a></button>                   
    {{ include('shows/_delete_form.html.twig') }}
{% endblock %}
