{% extends 'base.html.twig' %}

{% block title %}Edit {{ show.Location }} show{% endblock %}

{% block body %}
    <h1>Edit {{ show.Location }} show</h1>

    {{ include('shows/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('shows_index') }}">back to list</a>

    {{ include('shows/_delete_form.html.twig') }}
{% endblock %}
